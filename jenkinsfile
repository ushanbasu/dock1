node
{
def mavenHome = tool name: "maven 3.8.4"
stage ('github pulling')
{
git branch: 'main', url: 'https://github.com/ushanbasu/dock1.git'
}
stage ('build')
{
sh "${mavenHome}/bin/mvn clean package"
}
stage('build image')
{
    sh 'docker build -t ushanbasu/tom:1 .'
}
}
